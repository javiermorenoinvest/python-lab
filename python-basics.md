# Python Basics - Print, Variables, and Input

Today I practiced the foundation of Python by working with `print()`, variables, strings, integers, and `input()`.

## What I practiced

- Used `print()` to display text in the terminal
- Created variables with numbers
- Created variables with strings
- Printed variables to see their stored values
- Used `input()` to collect information from the user
- Combined variables, strings, and input to make small Python examples

## Key things I learned

### Print

`print()` is used to show something on the screen.

Example:

```python
print("Hello, world!")
